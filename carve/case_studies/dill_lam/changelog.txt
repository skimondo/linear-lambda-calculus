- new algebra with omega, new join operation (mult.bel), obviously
updated proof of the monoidal ops (comm, assoc, func and canc)

- def of unrestricted ctx as one whose labels are *not* lin

- still use unr for the  empty linear ctx int app rule

- updated proofs: merge_getid  merge_exch_exp

- subst lemma for the int case 
